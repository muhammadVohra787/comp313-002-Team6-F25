# How to run

1.Navigate to:
'
cd client
npm install
npm run build
'

Go to Chrome → Extensions → Manage Extensions → Load unpacked

Load the /client/dist folder -> This should load your extension.

Sign in with your Google account to begin

`https://developer.chrome.com/docs/extensions/get-started/tutorial/hello-world`
