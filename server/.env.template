# Connection string for your MongoDB instance.
# Example (local default): mongodb://localhost:27017/
MONGO_URI=

# Secret key used to sign JWTs for your app.
# Use a long, random string and KEEP THIS PRIVATE.
JWT_SECRET=

# API key for Google Gemini (only if you use Gemini features).
# Leave empty if you don't use it or configure it elsewhere.
GEMINI_API_KEY=

# Google OAuth 2.0 Web application client ID.
# From Google Cloud Console > Credentials > OAuth 2.0 Client IDs (Web application).
GOOGLE_CLIENT_ID=

# Google OAuth 2.0 Web application client secret.
# From the same place as the client ID. KEEP THIS PRIVATE.
GOOGLE_CLIENT_SECRET=

# OAuth redirect URL that Google will send users back to after login.
# Must exactly match one of the Authorized redirect URIs in your OAuth client.
# Example (local dev): http://localhost:5000/auth/google/callback
GOOGLE_REDIRECT_URI=